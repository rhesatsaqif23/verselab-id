// Screen dispatcher: maps any engine Screen to the matching domain renderer.
// Import the domain registry — never import domain renderers directly.
export { renderScreen } from "#/domains/registry.tsx";
